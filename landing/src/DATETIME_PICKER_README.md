# iOS-Style Date & Time Picker для Saturway

## 📱 Обзор

Профессиональный компонент выбора даты и времени с iOS-style wheel picker эффектом, интегрированный в океанскую тематику Saturway. Полностью адаптированный под Telegram Mini App с плавными анимациями и интуитивным UX.

## ✨ Основные возможности

### DateTimePicker Component
- **iOS-style wheel picker** с drag & drop взаимодействием
- **3 режима работы**: только дата, только время, дата+время
- **Smooth animations** с Motion/React
- **Умное форматирование**: автоматическое отображение "Сегодня", "Завтра"
- **Интервал минут**: 5 минут для удобства выбора
- **Диапазон дат**: выбор на 30 дней вперёд
- **Океанская палитра**: #4A9FD8 (дата), #52C9C1 (время)
- **Gradient overlays** для создания эффекта глубины
- **Полная локализация** на русский и английский языки

### TaskCreationModal Component
- **Многошаговое создание задачи** с плавными переходами
- **Полная форма задачи**: название, описание, дата/время, приоритет, напоминание
- **Визуальные индикаторы приоритета**: 🔴 Высокий, 🟡 Средний, 🟢 Низкий
- **Toggle напоминания** с анимированным переключателем
- **Backdrop с blur эффектом** для фокусировки на модале
- **Responsive layout** для мобильных устройств
- **Валидация полей** перед созданием

## 🎨 Дизайн

### Цветовая схема
```typescript
const oceanColors = {
  date: '#4A9FD8',      // Голубой океан
  time: '#52C9C1',      // Бирюзовый
  accent: '#5AB5E8',    // Светло-голубой
  warning: '#FFD93D',   // Жёлтый для напоминаний
  high: '#ef4444',      // Красный для высокого приоритета
  medium: '#4A9FD8',    // Голубой для среднего
  low: '#94a3b8',       // Серый для низкого
};
```

### Анимации
- **Spring animations** для естественности движения
- **Fade in/out** для модального окна
- **Scale transitions** для элементов
- **Gradient overlays** для wheel picker
- **Opacity transformations** для неактивных элементов

## 📦 Компоненты

### 1. DateTimePicker.tsx
Основной компонент для выбора даты и времени.

**Props:**
```typescript
interface DateTimePickerProps {
  value: Date;                              // Текущее значение
  onChange: (date: Date) => void;          // Callback при изменении
  mode?: 'date' | 'time' | 'datetime';     // Режим работы (по умолчанию: 'datetime')
}
```

**Использование:**
```tsx
import { DateTimePicker } from './components/DateTimePicker';

function MyComponent() {
  const [selectedDate, setSelectedDate] = useState(new Date());

  return (
    <DateTimePicker
      value={selectedDate}
      onChange={setSelectedDate}
      mode="datetime"
    />
  );
}
```

### 2. TaskCreationModal.tsx
Модальное окно для создания задачи с интегрированным picker.

**Props:**
```typescript
interface TaskCreationModalProps {
  isOpen: boolean;
  onClose: () => void;
  onCreateTask: (task: NewTask) => void;
}

interface NewTask {
  title: string;
  description?: string;
  dueDate?: Date;
  priority?: 'low' | 'medium' | 'high';
  reminder?: boolean;
}
```

**Использование:**
```tsx
import { TaskCreationModal } from './components/TaskCreationModal';

function MyComponent() {
  const [isOpen, setIsOpen] = useState(false);

  const handleCreateTask = (task: NewTask) => {
    console.log('New task:', task);
    // Сохранить задачу...
  };

  return (
    <TaskCreationModal
      isOpen={isOpen}
      onClose={() => setIsOpen(false)}
      onCreateTask={handleCreateTask}
    />
  );
}
```

### 3. WheelPicker (внутренний компонент)
iOS-style wheel component для прокрутки значений.

**Features:**
- Drag & drop для выбора
- Автоматическое центрирование
- Плавные spring animations
- Opacity & scale transitions
- Gradient overlays сверху и снизу

## 🌍 Локализация

Все тексты полностью локализованы в `LanguageContext.tsx`:

### Русский язык
```typescript
'taskModal.createTask': 'Новая задача',
'taskModal.taskTitle': 'Название задачи',
'taskModal.selectDateTime': 'Выбрать дату и время',
'datePicker.today': 'Сегодня',
'datePicker.tomorrow': 'Завтра',
```

### English
```typescript
'taskModal.createTask': 'New Task',
'taskModal.taskTitle': 'Task Title',
'taskModal.selectDateTime': 'Select Date and Time',
'datePicker.today': 'Today',
'datePicker.tomorrow': 'Tomorrow',
```

## 🔄 Интеграция в TodayScreen

Обновлённый `TodayScreen.tsx` теперь включает:
1. State для модального окна
2. Функцию `createTask` для обработки новых задач
3. Расширенный тип `Task` с дополнительными полями
4. Кнопку для открытия модала

```tsx
// В TodayScreen.tsx
const [isModalOpen, setIsModalOpen] = useState(false);

const createTask = (taskData: NewTask) => {
  const newTaskItem: Task = {
    id: Date.now().toString(),
    title: taskData.title,
    completed: false,
    dueDate: taskData.dueDate,
    priority: taskData.priority,
    description: taskData.description,
  };
  setTasks([newTaskItem, ...tasks]);
};

// Рендер
<TaskCreationModal
  isOpen={isModalOpen}
  onClose={() => setIsModalOpen(false)}
  onCreateTask={createTask}
/>
```

## 🎯 UX детали

### Wheel Picker поведение
- **Высота контейнера**: 240px для комфортного выбора
- **Высота элемента**: 48px (ITEM_HEIGHT)
- **Drag constraints**: ограничены количеством элементов
- **Elastic drag**: 0.1 для естественности
- **Spring animation**: stiffness: 300, damping: 30

### Форматирование даты
```typescript
// Умное отображение
Сегодня: "Сегодня, 14:30"
Завтра: "Завтра, 10:00"
Другие дни: "пн, 23 янв, 15:45"
```

### Приоритеты задач
- 🔴 **Высокий**: красный цвет (#ef4444)
- 🟡 **Средний**: океанский голубой (#4A9FD8)
- 🟢 **Низкий**: серый (#94a3b8)

## 📱 Демо компонент

`DateTimePickerDemo.tsx` предоставляет полный демонстрационный интерфейс:

1. **Standalone Picker** - изолированный picker для тестирования
2. **Date Only** - режим только даты
3. **Time Only** - режим только времени
4. **Task Creation Modal** - полный flow создания задачи
5. **Features List** - список всех возможностей

### Запуск демо
```tsx
// В App.tsx установите:
const PICKER_DEMO = true;
```

## 🚀 Технологии

- **Motion/React** - плавные анимации
- **TypeScript** - типобезопасность
- **Tailwind CSS** - стилизация
- **Lucide React** - иконки
- **React Hooks** - state management

## 📋 Чеклист функционала

- ✅ iOS-style wheel picker с drag & drop
- ✅ Smooth spring animations
- ✅ 3 режима работы (дата/время/оба)
- ✅ Океанская цветовая палитра
- ✅ Умное форматирование дат
- ✅ Интервал минут 5 мин
- ✅ Выбор на 30 дней вперёд
- ✅ Полная локализация (RU/EN)
- ✅ Модальное окно создания задачи
- ✅ Приоритеты задач
- ✅ Напоминания с toggle
- ✅ Валидация полей
- ✅ Responsive design
- ✅ Backdrop с blur
- ✅ Демо компонент

## 🎨 Следующие шаги

Потенциальные улучшения:
- [ ] Поддержка тёмной темы
- [ ] Haptic feedback для Telegram WebApp
- [ ] Повторяющиеся задачи (recurring)
- [ ] Быстрый выбор времени (утро/день/вечер)
- [ ] Календарный вид для выбора даты
- [ ] Celebration анимация при создании задачи
- [ ] Swipe to dismiss для модала

## 📖 Примеры использования

### Простой выбор даты
```tsx
<DateTimePicker
  value={date}
  onChange={setDate}
  mode="date"
/>
```

### Только время
```tsx
<DateTimePicker
  value={time}
  onChange={setTime}
  mode="time"
/>
```

### Полная форма задачи
```tsx
<TaskCreationModal
  isOpen={showModal}
  onClose={() => setShowModal(false)}
  onCreateTask={(task) => {
    console.log('Created:', task);
    // Сохранить в state или API
  }}
/>
```

## 🤝 Вклад

Компоненты созданы специально для Saturway с учётом:
- Океанской тематики (#4A9FD8, #52C9C1, #5AB5E8)
- Telegram Mini App UX guidelines
- iOS Human Interface Guidelines
- Локализация на RU/EN
- Accessibility best practices

---

**Создано для Saturway** - AI-органайзер для продуктивности 🌊
