# 🚀 Saturway Landing - Quick Start Guide

## Быстрый запуск

### 1. Включить лендинг
```typescript
// В /App.tsx
const LANDING_MODE = true;  // 👈 Установите true
const PICKER_DEMO = false;
```

### 2. Просмотр в браузере
Лендинг автоматически откроется с океанским фоном и всеми анимациями.

## 📱 Структура лендинга

```
┌─────────────────────────────────────┐
│      Navigation Bar (fixed)         │  ← Logo, Links, Language, CTA
├─────────────────────────────────────┤
│                                     │
│         Hero Section                │  ← Заголовок, Stats, Phone Mockup
│      (анимированные частицы)        │
│                                     │
├─────────────────────────────────────┤
│                                     │
│      Features Section               │  ← 4 карточки с возможностями
│         (4 columns)                 │
│                                     │
├─────────────────────────────────────┤
│                                     │
│    How It Works Section             │  ← 3 шага (с номерами)
│         (3 columns)                 │
│                                     │
├─────────────────────────────────────┤
│                                     │
│      Benefits Section               │  ← 2 колонки (текст + визуал)
│      (2 columns)                    │
│                                     │
├─────────────────────────────────────┤
│                                     │
│    Testimonials Section             │  ← 3 отзыва с рейтингами
│         (3 columns)                 │
│                                     │
├─────────────────────────────────────┤
│                                     │
│        CTA Section                  │  ← Email форма
│    (центрированная карточка)        │
│                                     │
├─────────────────────────────────────┤
│                                     │
│          Footer                     │  ← 4 колонки ссылок
│                                     │
└─────────────────────────────────────┘
```

## 🎨 Кастомизация за 5 минут

### Изменить статистику
```typescript
// В /components/Landing.tsx, строка ~71
const stats = [
  { value: '20K+', label: t('landing.stats.users') },      // 👈 Ваши цифры
  { value: '100K+', label: t('landing.stats.tasks') },
  { value: '98%', label: t('landing.stats.satisfaction') },
  { value: '5.0', label: t('landing.stats.rating') },
];
```

### Изменить отзывы
```typescript
// Строка ~85
const testimonials = [
  {
    name: 'Ваше Имя',              // 👈 Имя клиента
    role: 'Ваша Роль',             // 👈 Должность
    avatar: '👤',                   // 👈 Emoji аватар
    text: 'Текст отзыва...',       // 👈 Отзыв
    rating: 5,                      // 👈 Рейтинг 1-5
  },
  // ... добавьте больше
];
```

### Изменить цвета бренда
```typescript
// Глобальная замена в файле
#4A9FD8 → #YOUR_PRIMARY    // Основной цвет
#52C9C1 → #YOUR_SECONDARY  // Вторичный цвет
#5AB5E8 → #YOUR_ACCENT     // Акцентный цвет
```

## 🌍 Переключение языка

Лендинг автоматически поддерживает RU/EN через переключатель в навигации.

### Добавить свой язык
```typescript
// В /components/LanguageContext.tsx
const translations = {
  ru: { ... },
  en: { ... },
  de: {  // 👈 Добавьте новый язык
    'landing.hero.title': 'Dein Titel...',
    // ... все ключи
  }
};
```

## 📧 Настройка Email формы

```typescript
// В /components/Landing.tsx, строка ~428
const handleSubmit = (e: React.FormEvent) => {
  e.preventDefault();
  
  // TODO: Интегрируйте с вашим сервисом
  // Mailchimp
  fetch('https://YOUR_MAILCHIMP_URL', {
    method: 'POST',
    body: JSON.stringify({ email }),
  });
  
  // ConvertKit
  // fetch('https://api.convertkit.com/v3/forms/...')
  
  // SendGrid
  // fetch('/api/subscribe', { ... })
};
```

## 🎬 Анимации вкл/выкл

### Отключить фоновые частицы
```typescript
// Строка ~117, закомментируйте:
{/* <div className="pointer-events-none absolute inset-0 overflow-hidden">
  {[...Array(20)].map((_, i) => ( ... ))}
</div> */}
```

### Отключить scroll анимации
```typescript
// Замените везде:
initial={{ opacity: 0, y: 20 }}
whileInView={{ opacity: 1, y: 0 }}

// На:
// (просто удалите эти props)
```

## 📱 Mobile Menu

Сейчас на mobile показывается только кнопка. Для добавления полного меню:

```typescript
// Добавьте state
const [mobileMenuOpen, setMobileMenuOpen] = useState(false);

// Добавьте кнопку
<button 
  className="md:hidden"
  onClick={() => setMobileMenuOpen(!mobileMenuOpen)}
>
  <Menu className="h-6 w-6" />
</button>

// Добавьте drawer/modal с ссылками
```

## 🖼️ Добавить реальные скриншоты

```typescript
// Импортируйте ваши изображения
import screenshot1 from './assets/screenshot1.png';
import screenshot2 from './assets/screenshot2.png';

// Замените в phone mockup (строка ~250)
<div className="relative aspect-[9/19.5] ...">
  <img src={screenshot1} alt="App" className="..." />
</div>
```

## 📊 Google Analytics

```typescript
// Добавьте в index.html или создайте useAnalytics hook
declare global {
  interface Window {
    gtag: any;
  }
}

// Tracking CTA clicks
<RippleButton 
  onClick={() => {
    window.gtag?.('event', 'cta_click', {
      event_category: 'engagement',
      event_label: 'hero_cta'
    });
  }}
>
```

## 🎯 A/B Testing

### Вариант A vs B заголовка
```typescript
const variant = Math.random() > 0.5 ? 'A' : 'B';

const heroTitle = variant === 'A' 
  ? 'Продуктивность без выгорания'
  : 'Достигай больше, уставай меньше';
```

## 🚀 Deployment Checklist

- [ ] Заменить placeholder тексты
- [ ] Добавить реальные отзывы
- [ ] Обновить статистику
- [ ] Настроить email интеграцию
- [ ] Добавить Google Analytics
- [ ] Проверить на всех устройствах
- [ ] Протестировать все ссылки
- [ ] Добавить meta tags (SEO)
- [ ] Добавить Open Graph images
- [ ] Настроить домен
- [ ] SSL сертификат
- [ ] Проверить скорость загрузки

## 📈 SEO Optimization

### Добавьте в index.html
```html
<head>
  <title>Saturway - AI Органайзер для Продуктивности</title>
  <meta name="description" content="Продуктивность без выгорания. AI-органайзер с трекингом энергии.">
  <meta name="keywords" content="продуктивность, задачи, AI, органайзер">
  
  <!-- Open Graph -->
  <meta property="og:title" content="Saturway - AI Органайзер">
  <meta property="og:description" content="Продуктивность без выгорания">
  <meta property="og:image" content="/og-image.jpg">
  
  <!-- Twitter Card -->
  <meta name="twitter:card" content="summary_large_image">
</head>
```

## 🎨 Дополнительные секции

### Pricing Section
```typescript
<section className="py-20 px-6">
  <div className="mx-auto max-w-7xl">
    <h2>Тарифы</h2>
    <div className="grid md:grid-cols-3 gap-8">
      {/* Free, Pro, Enterprise */}
    </div>
  </div>
</section>
```

### FAQ Accordion
```typescript
import { Accordion } from './ui/accordion';

<section className="py-20 px-6">
  <h2>Частые вопросы</h2>
  <Accordion type="single" collapsible>
    <AccordionItem value="item-1">
      <AccordionTrigger>Вопрос 1?</AccordionTrigger>
      <AccordionContent>Ответ 1</AccordionContent>
    </AccordionItem>
  </Accordion>
</section>
```

## 🔗 Полезные ссылки

- **Компонент**: `/components/Landing.tsx`
- **Переводы**: `/components/LanguageContext.tsx`
- **Документация**: `/LANDING_PAGE_README.md`
- **App**: `/App.tsx` (установите `LANDING_MODE = true`)

## 💡 Pro Tips

1. **Scroll Smoothness**: Используйте `scroll-behavior: smooth` в CSS
2. **Images**: Оптимизируйте все изображения (TinyPNG, ImageOptim)
3. **Fonts**: Используйте system fonts для быстрой загрузки
4. **Animations**: Меньше анимаций = быстрее загрузка
5. **Mobile First**: 70% трафика с мобильных
6. **CTA Copy**: A/B тестируйте текст кнопок
7. **Social Proof**: Реальные отзывы лучше придуманных
8. **Loading**: Добавьте loading state для формы

## 🆘 Troubleshooting

### Анимации не работают
- Проверьте импорт: `import { motion } from 'motion/react'`
- Проверьте, что компонент обернут в `<LanguageProvider>`

### Переводы не отображаются
- Убедитесь, что добавили ключи в оба языка (ru и en)
- Проверьте синтаксис в `translations` объекте

### Layout сломан на mobile
- Проверьте responsive классы: `md:`, `lg:`
- Используйте Chrome DevTools для тестирования

### Фоновые частицы лагают
- Уменьшите количество: `[...Array(10)]` вместо 20
- Увеличьте `duration` анимации

---

**Быстрый старт завершен!** 🎉

Теперь у вас есть полностью функциональный лендинг для Saturway.

Вопросы? Смотрите `/LANDING_PAGE_README.md` для детальной документации.
